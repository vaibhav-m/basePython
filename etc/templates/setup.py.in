from setuptools import setup, find_packages

setup(
    name="${project-name}",
    version="${project-version}",
    url='${project-url}',
    license='${project-license}',
    description="A Django Project - ${project-name}",
    author='${author}',
    packages=find_packages('src'),
    package_dir={'': 'src'},
    install_requires=['setuptools', 'Django'],
)
